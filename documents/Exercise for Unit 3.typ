#strong[Exercise for Unit 3]

Name: Date:

Year and Section:

+ \(10 points) Use the Wikipedia python module and access any topic, as
  you will use that as your corpus, with a word limit of 1000 words.

Example usage:

#box(width: 4.386591207349081in, image("media/image1.png"))

#block[
#set enum(numbering: "1.", start: 2)
+ \(60 points) Train 2 models: a Bigram and Trigram Language Model, use
  the shared code as reference for bigram modeling, and update it to
  support trigrams.

+ \(30 points) Using a test sentence “#emph[The quick brown fox jumps
  over the lazy dog near the bank of the river.];” OR generate your own
  test sentence, create a function that will determine the perplexity
  score for each trained model.

  #block[
  #set enum(numbering: "a.", start: 1)
  + Bigram model perplexity -\> Test Sentence “” -\> Score:

  + Trigram model perplexity -\> Test Sentence “” -\> Score:
  ]
]

#quote(block: true)[
Upload your output in a public Github Repository named
CCS249-EXERCISE-3-LASTNAME and attach the Github link here:
]
